# Secrets — NEVER commit
.env
*.env
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
*.so
.Python
venv/
env/
.venv/

# Journal data — tracked here (synced by GitHub Actions from trading-bot artifacts)
# journal/trades.jsonl  ← committed by sync-journal workflow
# journal/bot.log       ← committed by sync-journal workflow

# OS
.DS_Store
Thumbs.db

# Test outputs
*.tmp
